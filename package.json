{
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i css/styles.css -o dist/styles.css --minify",
    "build": "npm run build:css && esbuild js/app.js --bundle --minify --sourcemap --outfile=dist/app.bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "tailwindcss": "^3.3.0"
  }
}